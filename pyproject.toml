[project]
name = "aeroforger-portfolio"
version = "0.1.0"
description = "AeroForger developer portfolio"
requires-python = ">=3.10"
dependencies = []

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.vercel]
entrypoint = "server.app:application"